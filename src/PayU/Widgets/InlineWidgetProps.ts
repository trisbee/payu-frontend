import Currency from "../enum/Currency";
import WidgetMode from "../enum/WidgetMode";
import WidgetType from "../enum/WidgetType";
import { SupportedLanguage } from "../enum/Language";
import BaseProps from "./BaseProps";

/**
 * 3.2 Inline widget
 *
 * Inline widget is a modification of the pop-up widget.
 * It does not contain the header with shop name and purchase amount and uses some additional parameters.
 *
 * See: http://developers.payu.com/en/payu_express.html#payu_express_inline_widget
 */
class InlineWidgetProps extends BaseProps
{

}

export default InlineWidgetProps;
